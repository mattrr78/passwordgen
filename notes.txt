Local Dev
    SCP
        scp passwordgen-1.0-SNAPSHOT.jar mrosenberger@192.168.1.106:~mrosenberger/passwordgen/build/libs
Docker
    List docker images: docker images

    Deploy and run:
        docker build . -t localhost:32000/password-gen:v1.0.1
        docker run -p 8080:8080 localhost:32000/password-gen:v1.0.1


    Distroless docker images exist
    Look into layered (exploded jar) approach vs. fat jar for improved performance
    Look into user/group instead of root
    Removing docker image:
        docker container prune
        docker rmi image/name
microk8s
	https://medium.com/manikkothu/build-and-deploy-apps-on-microk8s-1df26d1ddd3c#:~:text=MicroK8s%20can%20not%20directly%20access,it%20into%20MicroK8s%20local%20registry.
	microk8s.enable registry
    docker push localhost:32000/password-gen
	kubectl create deployment password-gen --image=localhost:32000/password-gen:v1.0.1
	kubectl scale --replicas=6 deployment.apps/password-gen
	kubectl expose deployment.apps/password-gen --port=8080 --type=NodePort

	curl --header "Content-Type: application/json" --request POST --data '{"count": 10,"minimumLength": 10}' http://<machine ip>:<service assigned port>/api/password
    kubectl get all
    kubectl delete <deployment>
	
	microk8s enable ha-cluster on each VM before joining
